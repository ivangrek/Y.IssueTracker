@model Y.IssueTracker.Web.Models.Issue.DeleteIssueViewModel

@{
    ViewData["Title"] = "Delete Issue";
}

<h1 class="mb-3">@ViewData["Title"]</h1>

<div class="card">
    <div class="card-body">
        <p class="card-text">Are you sure you want to delete this?</p>

        <form asp-controller="Issue" asp-action="Delete" method="post">
            @Html.HiddenFor(x => x.Id)

            <div class="mb-3 row">
                <label asp-for="Name" class="col-sm-2 col-form-label"></label>

                <div class="col-sm-10">
                    <div class="col-form-label">@Model.Name</div>
                </div>
            </div>

            <button type="submit" class="btn btn-danger">Delete</button>
            <a asp-area="" asp-controller="Issue" asp-action="Index" class="btn btn-link">Cancel</a>
        </form>
    </div>
</div>
